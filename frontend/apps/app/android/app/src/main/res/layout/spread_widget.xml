<?xml version="1.0" encoding="utf-8"?>
<!-- #E2D6BE/#0A0A0A are the default theme's spreadCanvas/foreground tokens (theme.ts's "Pyxie
     (Default)" seed, converted from OKLCH to sRGB) - fixed rather than following the user's active
     theme, since RemoteViews has no reach into the webview's CSS custom properties. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E2D6BE"
    android:padding="16dp">

    <LinearLayout
        android:id="@+id/widget_message_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/widget_logo"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginBottom="8dp"
            android:src="@drawable/ic_stat_notify"
            android:tint="#0A0A0A"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="#0A0A0A"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:gravity="center"
            android:textColor="#0A0A0A"
            android:alpha="0.7"
            android:textSize="11sp" />

    </LinearLayout>

    <ImageView
        android:id="@+id/widget_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:visibility="gone"
        android:contentDescription="@null" />

</FrameLayout>
